# 0.3.0 发布说明核查

2026-09-19，根据用户要求改写发布页并补充初学者、agent 安装说明。文案先按 humanizer 的事实保留和模板表达检查，再按 humanizer-zh 做中文说明文润色；不改写命令、版本号、SHA-256 或测试边界。

## 本次实际检查

- 原发布 ZIP 校验：RELEASE_VERIFIED。
- 临时 Skill 根全新安装：installed，退出码 0。
- 按新说明使用 `--entry-mode discovery` 初始化临时项目：退出码 0。
- 验证临时项目：PASS，0 warning(s)。
- 发布工具测试：10/10 passed。
- 补充依赖锁分别验证两个现有 Skill 根的中文、英文依赖：4/4 passed。
- 发布包及 0.3.0 tag 未重建或覆盖。

## 修正与边界

原安装说明误写 `--mode`，已在 main 文档与发布页修正；ZIP 中旧文档仍保持发布时字节，用户应按新说明使用 `--entry-mode`。

实际运行发布后脚本时，包内精简依赖锁会报 `dependency lock uses an unsupported hash algorithm`，且默认锁文件定位不适配单 Skill 布局。已补充不含个人路径的已验证锁文件，要求使用 `--dependency-lock` 显式传入。这个补充文件只恢复匹配本轮已验证依赖的检查入口，不证明任意上游最新版兼容。完整语言质量仍需实际调用语言技能并做人工语义复核。

补充文件 `dependencies.verified-0.3.0.json` 的 SHA-256 为 `cf08320018905a44d64e19e05cd80f833c168109a69fc83012ed62122821ebdf`（发布附件按本次本地字节上传）。

历史 96 项测试和 24 个场景记录继续保留，但已经明确注明它们没有覆盖这次发现的发布布局问题，不能据此声称任意新环境开箱即用。
